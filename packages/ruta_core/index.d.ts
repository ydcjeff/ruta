// types defined in `types.d.ts` are in global scope of this package
// so reexport the necessary ones for public API
export {
	AnyObj,
	NavigationHook,
	NavigationHookArgs,
	Register,
	RegisteredRouter,
	RegisteredRoutes,
	Route,
	RouteOptions,
	Ruta,
	RutaOptions,
	create_routes,
	define_route,
} from './types';
